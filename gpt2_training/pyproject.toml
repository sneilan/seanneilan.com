[project]
name = "gpt2-training"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "datasets>=4.8.2",
    "torch>=2.10.0",
    "transformers>=5.3.0",
]
